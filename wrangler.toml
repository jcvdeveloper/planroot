name = "planroot"
main = "src/index.ts"
compatibility_date = "2026-08-17"

# Servir arquivos estáticos do /site/
[site]
bucket = "site"

# Variáveis de ambiente (configure via Cloudflare Dashboard)
[env.production]
vars = { ENVIRONMENT = "production" }
